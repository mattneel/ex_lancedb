# Livebook Rule

- Keep a runnable Livebook example in `livebooks/quickstart.livemd`.
- The notebook should cover connect, create table, insert, search, and create index.
- Keep the example dataset small enough for quick local execution.
- Keep prerequisites explicit (`rustc`, `cargo`, `protoc`).
